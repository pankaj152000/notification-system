export enum NotificationChannel {
  email = 'email',
  sms = 'sms',
  push = 'push',
}

export enum NotificationStatus {
  pending = 'pending',
  sent = 'sent',
  failed = 'failed',
  processing = 'processing',
}
